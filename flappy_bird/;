var bird;

function setup() {
    createCanvas(640, 800);
    background(51);

    bird = new Bird();
    bird.x = 64;
}

function draw() {
    bird.show();
    bird.update();
}

function keyPressed() {

    if (key === " " {

        bird.flap();

    }

}
